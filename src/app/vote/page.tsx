import React from "react";

import styles from "./styles.module.sass";

type Props = {};

const Vote = (props: Props) => {
	return <div className={styles.aboutWrapper}>Vote</div>;
};

export default Vote;
