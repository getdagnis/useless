import React from "react";

import styles from "./styles.module.sass";

type Props = {};

const Suggest = (props: Props) => {
	return <div className={styles.aboutWrapper}>Suggest</div>;
};

export default Suggest;
