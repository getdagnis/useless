import React from "react";

import styles from "./styles.module.sass";

type Props = {};

const FAQ = (props: Props) => {
	return <div className={styles.aboutWrapper}>FAQ</div>;
};

export default FAQ;
