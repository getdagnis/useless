import React from "react";

import styles from "./styles.module.sass";

type Props = {};

const Publish = (props: Props) => {
	return <div className={styles.aboutWrapper}>Publish</div>;
};

export default Publish;
