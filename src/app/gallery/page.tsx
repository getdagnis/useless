import React from "react";

import styles from "./styles.module.sass";

type Props = {};

const Gallery = (props: Props) => {
	return <div className={styles.aboutWrapper}>Gallery</div>;
};

export default Gallery;
