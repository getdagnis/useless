import React from "react";

import styles from "./styles.module.sass";

type Props = {};

const Roadmap = (props: Props) => {
	return <div className={styles.aboutWrapper}>Roadmap</div>;
};

export default Roadmap;
